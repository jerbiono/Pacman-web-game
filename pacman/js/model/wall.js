/**
 * A wall is a fixed Pacman maze component.
 */
class Wall extends Tile{

     /**
     * To be created, a Wall just need an id.
     *
     * @param {string} id unique Wall's id
     */
    constructor(id){
        super(id);
    }
}